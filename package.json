{
  "name": "damiancampos-dev",
  "version": "1.0.0",
  "description": "Personal portfolio — MERN stack monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "node server/app.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
